package mrs;

import javafx.application.Application;

public class Main {
    //Rozwiązać równanie -u''+u'+2u=x^3+x, x'(0)=1,x'(1)+x(1)=0 na przedziale [0,1]
    public static void main(String[] args) {
        Application.launch(App.class,args);
    }
}